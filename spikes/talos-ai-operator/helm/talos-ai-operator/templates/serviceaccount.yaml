{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "talos-ai-operator.serviceAccountName" . }}
  labels:
    {{- include "talos-ai-operator.labels" . | nindent 4 }}
{{- end }}
