apiVersion: v1
kind: Namespace
metadata:
  name: exp-chdb-parquet
  labels:
    app.kubernetes.io/part-of: exp-chdb-parquet
    # This spike runs an unprivileged python + MinIO pod; baseline PodSecurity
    # (the cluster default) is sufficient, so we don't relabel the namespace.
